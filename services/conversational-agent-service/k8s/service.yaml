apiVersion: v1
kind: Service
metadata:
  name: conversational-agent-service
spec:
  selector:
    app: conversational-agent-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: ClusterIP
